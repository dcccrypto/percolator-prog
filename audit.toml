# Security audit exceptions — documented suppression of dev-only advisories
# Generated: 2026-03-23
# Reviewed by: coder (Forge)
#
# All four advisories below exist ONLY in dev-dependencies (solana-sdk, solana-client, litesvm).
# The on-chain BPF binary (`cargo tree -e normal`) has ZERO exposure to these crates.
# These will be resolved when we upgrade to Solana SDK 3.x+ (tracked in GH#1591).

[advisories]
ignore = [
    # RUSTSEC-2022-0093: ed25519-dalek <2.0 double public key signing oracle
    # Source: solana-keypair 2.2.1 → ed25519-dalek-bip32 0.2.0 → ed25519-dalek 1.0.1
    # Impact: DEV-ONLY (litesvm, solana-sdk, solana-client). Not in on-chain binary.
    # Fix: Requires Solana SDK 3.x+ (ed25519-dalek-bip32 0.3.0 uses ed25519-dalek 2.x)
    "RUSTSEC-2022-0093",

    # RUSTSEC-2024-0344: curve25519-dalek <4.1.3 timing variability
    # Source: ed25519-dalek 1.0.1 → curve25519-dalek 3.2.0 (same dev-only chain)
    # Impact: DEV-ONLY. Production deps use curve25519-dalek 4.1.3 (patched).
    "RUSTSEC-2024-0344",

    # RUSTSEC-2026-0049: rustls-webpki <0.103.10
    # Source: dev-dep chain via solana-client → rustls → rustls-webpki
    # Impact: DEV-ONLY. Not in on-chain binary.
    "RUSTSEC-2026-0049",

    # RUSTSEC-2021-0139: ansi_term unmaintained
    # Source: deep transitive dev-dep
    # Impact: DEV-ONLY. No security issue, just unmaintained.
    "RUSTSEC-2021-0139",

    # RUSTSEC-2021-0145: atty 0.2.14 potential unaligned read (unsound)
    # Source: env_logger → solana-logger → solana-genesis-config → litesvm (dev-only chain)
    # Impact: DEV-ONLY. Not in on-chain binary.
    "RUSTSEC-2021-0145",

    # ── New unmaintained-crate warnings (2026-03-30 deep audit) ──
    # All 6 are in the solana-sdk/solana-client/litesvm dev-only chain.
    # None reach the on-chain BPF binary (confirmed via cargo tree -e normal).
    # All classified as "unmaintained" only — no active CVE or exploit path.
    # Will be resolved when Solana SDK upgrades to 3.x+ (GH#1591).

    # RUSTSEC-2024-0375: atty 0.2.14 (second advisory — same package as RUSTSEC-2021-0145)
    # Source: env_logger → solana-logger → litesvm (dev-only)
    "RUSTSEC-2024-0375",

    # RUSTSEC-2025-0141: bincode 1.3.3 unmaintained
    # Source: solana-zk-token-sdk → spl-token-2022 (dev dep chain)
    "RUSTSEC-2025-0141",

    # RUSTSEC-2024-0388: derivative 2.2.0 unmaintained
    # Source: ark-poly → ark-ec → solana-bn254 → solana-sdk (dev-only)
    "RUSTSEC-2024-0388",

    # RUSTSEC-2025-0119: number_prefix 0.4.0 unmaintained
    # Source: indicatif → solana-tpu-client → solana-client (dev-only)
    "RUSTSEC-2025-0119",

    # RUSTSEC-2024-0436: paste 1.0.15 unmaintained
    # Source: ark-ff → solana-bn254 → solana-sdk (dev-only)
    "RUSTSEC-2024-0436",

    # RUSTSEC-2025-0134: rustls-pemfile 1.0.4 unmaintained
    # Source: reqwest → solana-rpc-client-api → solana-client (dev-only)
    "RUSTSEC-2025-0134",
]
